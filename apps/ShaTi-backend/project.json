{
  "name": "backend",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm dev",
        "cwd": "apps/ShaTi-backend"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm build",
        "cwd": "apps/ShaTi-backend"
      }
    },
    "lint": {
      "executor": "nx-oxlint:lint"
    }
  }
}